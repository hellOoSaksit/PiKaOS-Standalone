{
  "name": "pikaos-redirectmap",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "URL Redirect Map — standalone old-site→new-site redirect mapping + IIS web.config generator (extracted from PiKaOs). No login, no nav.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
